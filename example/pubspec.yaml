name: imagekit_ffi_example
description: "Demonstrates how to use the imagekit_ffi plugin."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  imagekit_ffi:
    path: ../

  cupertino_icons: ^1.0.8
  camera: 0.11.0+2
  camera_android_camerax: 0.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
